{
  "name": "savemyride-backend",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "sync-shared": "node scripts/sync-shared.js",
    "pretypecheck": "npm run sync-shared",
    "typecheck": "tsc --noEmit",
    "prebuild": "npm run sync-shared",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.687.0",
    "@aws-sdk/client-dynamodb": "^3.687.0",
    "@aws-sdk/lib-dynamodb": "^3.687.0",
    "esbuild": "^0.24.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0"
  }
}
